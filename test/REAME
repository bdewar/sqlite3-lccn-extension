testing
========================


@todo
  Basically:
   - INSERT a few poorly-formed callnumbers that have non-trivial sort order
   - normalize them
   - sort them
   - print them out
   - compare print out with expected print out
        (compare for sort order and normalized verion of input)

